#include <stdio.h>

int main()
{
    int N, x, y=1, count;
    scanf ("%d", &N);
    if (Ν>=1)
    {
        printf("2\n");
    }
    for (count=2; count<=N;)
    {
        for(x=2; x<=1;)
    }
}
